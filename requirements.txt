# ============================================================================
# JARVIS - Voice Assistant Requirements
# Python 3.10+
#
# Wersje podane jako minimalne (>=), nie sztywne (==) — sztywne piny nie mają
# gotowych paczek dla nowszych wydań Pythona i wywracają całą instalację.
#
#   pip install -r requirements.txt
# ============================================================================

# ---------------------------------------------------------------------------
# ROZPOZNAWANIE MOWY
# ---------------------------------------------------------------------------
SpeechRecognition>=3.10.0     # Klient darmowego endpointu Google Speech
pyaudio>=0.2.14               # Wejście z mikrofonu

# ---------------------------------------------------------------------------
# SYNTEZA MOWY
# ---------------------------------------------------------------------------
pyttsx3>=2.90                 # TTS offline (SAPI5 na Windows)

# ---------------------------------------------------------------------------
# AUTOMATYZACJA PRZEGLĄDARKI (YouTube)
# ---------------------------------------------------------------------------
selenium>=4.15.0              # Sterowanie Chrome
webdriver-manager>=4.0.1      # Automatyczne pobieranie ChromeDrivera

# ---------------------------------------------------------------------------
# AUTOMATYZACJA PULPITU (Discord)
# ---------------------------------------------------------------------------
pyautogui>=0.9.54             # Skróty klawiszowe i klikanie
pygetwindow>=0.0.9            # Odnajdywanie okna Discorda
pywinauto>=0.6.8; sys_platform == 'win32'   # UI Automation: mute osoby, podgląd ekranu
pywin32>=306; sys_platform == 'win32'       # Wymagane przez pyttsx3 (SAPI5) i pywinauto

# ---------------------------------------------------------------------------
# INTERFEJS GRAFICZNY
# ---------------------------------------------------------------------------
PyQt6>=6.6.0

# ---------------------------------------------------------------------------
# LOGOWANIE
# ---------------------------------------------------------------------------
colorlog>=6.8.0               # Kolorowe logi (opcjonalne — bez niego też działa)

# ---------------------------------------------------------------------------
# TESTY
# ---------------------------------------------------------------------------
pytest>=7.4.0

# ============================================================================
# OPCJONALNE — pod przyszłe funkcje, celowo NIE instalowane domyślnie.
# Odkomentuj dopiero, gdy dana funkcja faktycznie powstanie.
# ============================================================================
# vosk>=0.3.45                # Rozpoznawanie mowy offline (zamiast Google)
# gTTS>=2.4.0                 # TTS online, naturalniejszy głos, wymaga internetu
# requests>=2.31.0            # Integracje HTTP (np. pogoda)
# spotipy>=2.23.0             # Sterowanie Spotify
# python-dotenv>=1.0.0        # Klucze API w pliku .env

# ============================================================================
# NARZĘDZIA DEWELOPERSKIE (nieobowiązkowe)
# ============================================================================
# pytest-cov>=4.1.0           # Pokrycie kodu testami
# black>=23.12.0              # Formatowanie
# flake8>=6.1.0               # Kontrola stylu

# ============================================================================
# UWAGI INSTALACYJNE
# ============================================================================
# Windows: pyaudio bywa kłopotliwy — gdy pip zawiedzie:
#     pip install pipwin && pipwin install pyaudio
# macOS:   brew install portaudio
# Linux:   sudo apt install portaudio19-dev python3-dev
